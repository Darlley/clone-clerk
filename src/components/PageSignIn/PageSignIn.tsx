'use client';

import SigninForm from '../SigninForm';
import { PageSignInProps } from './PageSignIn.types';
export default function PageSignIn(props: PageSignInProps) {
  // const {} = props
  return <SigninForm />;
}
